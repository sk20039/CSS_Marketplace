{
  "name": "escrow-service",
  "version": "1.0.0",
  "description": "Escrow payment service for USA Cricket Marketplace (C2C resale) - order state machine, Stripe Connect escrow, dispute handling.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "migrate": "node-pg-migrate -m migrations up",
    "migrate:down": "node-pg-migrate -m migrations down",
    "test": "node tests/idempotency.test.js && node tests/recovery.test.js && node tests/admin.test.js && node tests/escrow.test.js && node tests/health.test.js"
  },
  "license": "UNLICENSED",
  "devDependencies": {
    "supertest": "^7.0.0"
  },
  "dependencies": {
    "better-sqlite3": "^13.0.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.3",
    "node-cron": "^3.0.3",
    "node-pg-migrate": "^7.0.0",
    "nodemailer": "^6.9.14",
    "pg": "^8.12.0",
    "stripe": "^16.12.0"
  }
}
