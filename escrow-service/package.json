{
  "name": "escrow-service",
  "version": "1.0.0",
  "description": "Escrow payment service for USA Cricket Marketplace (C2C resale) - order state machine, Stripe Connect escrow, dispute handling.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "migrate": "node-pg-migrate -m migrations up",
    "migrate:down": "node-pg-migrate -m migrations down",
    "install:prod": "npm ci --omit=dev",
    "test:smoke": "node tests/smoke.test.js",
    "test": "node tests/fee.test.js && node tests/idempotency.test.js && node tests/recovery.test.js && node tests/admin.test.js && node tests/escrow.test.js && node tests/health.test.js && node tests/config.test.js && node tests/cors.test.js && node tests/security.test.js && node tests/shipping.test.js && node tests/smoke.test.js"
  },
  "license": "UNLICENSED",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^8.6.2",
    "jsonwebtoken": "^9.0.3",
    "node-cron": "^3.0.3",
    "node-pg-migrate": "^7.0.0",
    "nodemailer": "^6.9.14",
    "pg": "^8.12.0",
    "stripe": "^16.12.0"
  },
  "devDependencies": {
    "supertest": "^7.0.0"
  }
}
